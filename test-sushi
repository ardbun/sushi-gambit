task.wait(1)
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")

local LocalPlayer
repeat
    LocalPlayer = Players.LocalPlayer
    task.wait()
until LocalPlayer

local Character
repeat
    Character = LocalPlayer.Character
    task.wait()
until Character

local ok = false
repeat
    ok = pcall(function()
        LocalPlayer.CharacterAdded:Connect(function(c)
            Character = c
        end)
    end)
    task.wait()
until ok

local GameData = ReplicatedStorage:WaitForChild("GameData")
local PlayerStats = LocalPlayer:WaitForChild("PlayerStats")
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Fetch offsets once
local offsets = HttpService:JSONDecode(game:HttpGet("https://imtheo.lol/Offsets/OffsetsHex.json"))["Offsets"]
local ScreenGuiEnabled = offsets["GuiObject"]["ScreenGui_Enabled"]
local HoldDuration     = offsets["ProximityPrompt"]["HoldDuration"]
local PromptEnabled    = offsets["ProximityPrompt"]["Enabled"]

local SETTINGS = {
    Auto = {
        Cooking        = { Enabled = false },
        Washing        = { Enabled = false, delay = 0.1 },
        ChloroformSpys = { Enabled = false }
    },
    Cheat = {
        Spoof   = { Greenbar = { Enabled = false }, SpongeEffectiveness = { Enabled = false } },
        Instant = { CleanDish = { Enabled = false }, SeatCustomer = { Enabled = true } }
    },
    Notify = {
        DineAndDash = { Enabled = false },
        AdminJoin   = { Enabled = false }
    }
}

local ESP_SETTINGS = {
    MAX_DIST    = 80,
    MAX_DIST_SQ = 80 * 80,
    Dasher          = { Enabled = false, Color = Color3.new(1, 0, 0) },
    Armed           = { Enabled = false, Color = Color3.new(1, 0, 0) },
    Police          = { Enabled = false, Color = Color3.new(0, 0, 1) },
    Critic          = { Enabled = false, Color = Color3.new(1, 1, 0) },
    HealthInspector = { Enabled = false, Color = Color3.new(1, 0, 0) }, -- key has no space
    Windows = { Enabled = false },
    Labels  = { day = false, waitinginline = false, companymoney = false,
                electricitybill = false, vegstock = false, fishstock = false,
                humanfleshstock = false, ricestock = false }
}

-- ─── Drawing Window Lib ────────────────────────────────────────────────────

local Lib = {}
Lib.__index = Lib

function Lib.new(titleText, width, height, cornerRadius)
    local self = setmetatable({}, Lib)

    local radius  = cornerRadius or 10
    local w       = width  or 650
    local h       = height or 400
    local startX, startY = 100, 100
    local bgColor = Color3.fromRGB(15, 15, 15)

    self.Elements    = {}
    self.Labels      = {}
    self.Radius      = radius
    self.Size        = Vector2.new(w, h)
    self.Position    = Vector2.new(startX, startY)
    self.InitialSize = Vector2.new(w, h)

    local function createCircle(pos)
        local c = Drawing.new("Circle")
        c.Position = pos; c.Radius = radius; c.Color = bgColor
        c.Filled = true; c.Visible = false; c.NumSides = 64; c.Thickness = 0
        table.insert(self.Elements, c)
        return c
    end

    local function createRect(pos, size)
        local r = Drawing.new("Square")
        r.Position = pos; r.Size = size; r.Color = bgColor
        r.Filled = true; r.Visible = false; r.Thickness = 0
        table.insert(self.Elements, r)
        return r
    end

    self.TopLeft      = createCircle(Vector2.new(startX + radius,     startY + radius))
    self.TopRight     = createCircle(Vector2.new(startX + w - radius, startY + radius))
    self.BottomLeft   = createCircle(Vector2.new(startX + radius,     startY + h - radius))
    self.BottomRight  = createCircle(Vector2.new(startX + w - radius, startY + h - radius))
    self.VerticalFill = createRect(Vector2.new(startX + radius, startY), Vector2.new(w - 2*radius, h))
    self.HorizontalFill = createRect(Vector2.new(startX, startY + radius), Vector2.new(w, h - 2*radius))

    local title = Drawing.new("Text")
    title.Text = titleText; title.Size = 12
    title.Position = Vector2.new(startX + 12, startY + 15)
    title.Color = Color3.fromRGB(255, 0, 0)
    title.Visible = false; title.Center = false; title.Outline = true
    self.Title = title
    table.insert(self.Elements, title)

    -- Drag logic — store connection so it can be disconnected
    local mouse = LocalPlayer:GetMouse()
    local dragging, dragOffset = false, Vector2.new(0, 0)

    self._dragConn = RunService.RenderStepped:Connect(function()
        local mx, my = mouse.X, mouse.Y
        local px, py = self.Position.X, self.Position.Y
        local sw, sh = self.Size.X, self.Size.Y

        if ismouse1pressed() then
            if not dragging then
                if mx >= px and mx <= px + sw and my >= py and my <= py + sh then
                    dragging   = true
                    dragOffset = Vector2.new(mx - px, my - py)
                end
            end
        else
            dragging = false
        end

        if dragging then
            self:Edit(Vector2.new(mx - dragOffset.X, my - dragOffset.Y))
        end
    end)

    return self
end

function Lib:Show(isVisible)
    for _, e in ipairs(self.Elements) do e.Visible = isVisible end
    for _, entry in ipairs(self.Labels) do entry.drawing.Visible = isVisible end
end

function Lib:Edit(newPosition, newSize)
    local pos  = newPosition or self.Position
    local size = newSize     or self.Size
    self.Position = pos; self.Size = size

    local x, y, w, h, r = pos.X, pos.Y, size.X, size.Y, self.Radius

    self.TopLeft.Position     = Vector2.new(x + r,     y + r)
    self.TopRight.Position    = Vector2.new(x + w - r, y + r)
    self.BottomLeft.Position  = Vector2.new(x + r,     y + h - r)
    self.BottomRight.Position = Vector2.new(x + w - r, y + h - r)

    self.VerticalFill.Position   = Vector2.new(x + r, y)
    self.VerticalFill.Size       = Vector2.new(w - 2*r, h)
    self.HorizontalFill.Position = Vector2.new(x, y + r)
    self.HorizontalFill.Size     = Vector2.new(w, h - 2*r)

    self.Title.Position = Vector2.new(x + 12, y + 15)

    for _, entry in ipairs(self.Labels) do
        entry.drawing.Position = pos + entry.relPos
    end
end

function Lib:Delete()
    if self._dragConn then self._dragConn:Disconnect() end
    for _, e in ipairs(self.Elements) do e:Remove() end
    for _, entry in ipairs(self.Labels) do entry.drawing:Remove() end
    self.Elements = {}
    self.Labels   = {}
end

local LABEL_SIZE       = 12
local LABEL_X          = 12
local LABEL_START_Y    = 32
local LABEL_STEP       = 18
local LABEL_BOTTOM_PAD = 15

function Lib:_refreshLabelPositions()
    for i, entry in ipairs(self.Labels) do
        local relPos = Vector2.new(LABEL_X, LABEL_START_Y + (i-1) * LABEL_STEP)
        entry.relPos = relPos
        entry.drawing.Position = self.Position + relPos
    end
    local neededH = LABEL_START_Y + #self.Labels * LABEL_STEP + LABEL_BOTTOM_PAD
    self:Edit(self.Position, Vector2.new(self.Size.X, math.max(neededH, self.InitialSize.Y)))
end

function Lib:AddLabel(text, color)
    local d = Drawing.new("Text")
    d.Text = text or ""; d.Size = LABEL_SIZE
    d.Color = color or Color3.fromRGB(255, 255, 255)
    d.Visible = true; d.Center = false; d.Outline = true

    local entry = { drawing = d, relPos = Vector2.new(0, 0) }
    table.insert(self.Labels, entry)
    self:_refreshLabelPositions()

    local lib = self
    local handle = {}

    function handle:SetText(t) d.Text = t end
    function handle:Edit(c) if c then d.Color = c end end
    function handle:Delete()
        d:Remove()
        for i, e in ipairs(lib.Labels) do
            if e == entry then table.remove(lib.Labels, i) break end
        end
        lib:_refreshLabelPositions()
    end

    return handle
end

-- ─── ESP Entity / Manager ─────────────────────────────────────────────────

local STATE = { ACTIVE = 1, DESTROYED = 2 }

local Entity = {}
Entity.__index = Entity

function Entity.new(instance, text, typeName)
    text.Text = typeName
    return setmetatable({
        Instance = instance,
        Head     = instance:FindFirstChild("Head"),
        Text     = text,
        Type     = typeName,
        State    = STATE.ACTIVE
    }, Entity)
end

function Entity:Update(pos, onScreen, enabled)
    if self.State == STATE.DESTROYED then return end
    if not enabled then self:Destroy() return end
    if not onScreen then
        if self.Text.Visible then self.Text.Visible = false end
        return
    end
    self.Text.Position = pos
    if not self.Text.Visible then self.Text.Visible = true end
end

function Entity:Destroy()
    if self.State == STATE.DESTROYED then return end
    self.State = STATE.DESTROYED
    pcall(function() self.Text:Remove() end)
end

function Entity:Hide()
    if self.Text then self.Text.Visible = false end
end

function Entity:SetColor(color)
    if self.Text then self.Text.Color = color; self.Text.Outline = true end
end

local Manager = { Entities = {}, List = {} }

function Manager:Add(id, entity)
    self.Entities[id] = entity
    table.insert(self.List, entity)
end

function Manager:Remove(id)
    local ent = self.Entities[id]
    if not ent then return end
    ent:Destroy()
    self.Entities[id] = nil
    for i = 1, #self.List do
        if self.List[i] == ent then table.remove(self.List, i) break end
    end
end

-- ─── NPC Cache ────────────────────────────────────────────────────────────

local folder       = game.Workspace.NpcDestination.SpawnedNPCs
local customer_list = {}
local npc_cache     = {}

-- Build and maintain npc_cache
task.spawn(function()
    while true do
        local seen = {}
        for _, npc in ipairs(folder:GetChildren()) do
            local addr = npc.Address
            seen[addr] = true
            if not npc_cache[addr] then
                npc_cache[addr] = { inst = npc, head = npc:FindFirstChild("Head") }
            end
        end
        for addr in pairs(npc_cache) do
            if not seen[addr] then
                -- Clean up customer_list when NPC leaves mid-day
                customer_list[addr] = nil
                Manager:Remove(addr)
                npc_cache[addr] = nil
            end
        end
        task.wait(0.5)
    end
end)

-- ─── Windows ──────────────────────────────────────────────────────────────

local Windows = Lib.new("Sushi Gambit", 150, 55)
Windows:Show(false)

local dayLabel, waitinginline, companyMoneyLabel = nil, nil, nil
local vegLabel, fishLabel, HumanFlesh, Rice      = nil, nil, nil, nil

-- ─── UI ───────────────────────────────────────────────────────────────────

local background = {
    'Intern','SofaSurfer','LineCook','TechEnthusiast',
    'Amish','Butcher','CoffeeFanatic','UsedCarSalesman',
    'Philanthropist','SelfAbsorbed'
}

UI.AddTab("Sushi Gambit", function(tab)
    local MainSec = tab:Section("Main", "Left", { "Auto", "Cheat", "Prompt" })

    if MainSec.page == 0 then
        MainSec:Toggle('auto_cooking', 'Auto Cooking', function(v)
            SETTINGS.Auto.Cooking.Enabled = v
        end)
        MainSec:Toggle('auto_washing', 'Auto DishWashing', function(v)
            SETTINGS.Auto.Washing.Enabled = v
        end)
        MainSec:SliderFloat('auto_washing_delay', 'Delay (ms)', 0.1, 2.0, 0.1, '%.1f', function(v)
            SETTINGS.Auto.Washing.delay = v
        end)
        MainSec:Toggle('auto_chloroform_spys', 'Auto Chloroform Spys', function(v)
            SETTINGS.Auto.ChloroformSpys.Enabled = v
        end)

    elseif MainSec.page == 1 then
        MainSec:Toggle('spoof_greenbar', 'Spoof Greenbar', function(v)
            SETTINGS.Cheat.Spoof.Greenbar.Enabled = v
        end)
        MainSec:Toggle('instant_clean_dish', 'Fast Clean Dish', function(v)
            SETTINGS.Cheat.Instant.CleanDish.Enabled = v
        end)
        MainSec:Toggle('spoof_sponge_effectiveness', 'Instant Sponge Effect', function(v)
            SETTINGS.Cheat.Spoof.SpongeEffectiveness.Enabled = v
        end)

    elseif MainSec.page == 2 then
        MainSec:Toggle('always_computer', 'Use computer during blackout', function(v)
            if not v then return end
            task.spawn(function()
                while UI.GetValue('always_computer') do
                    local prompt = game.Workspace.RestaurantArea.restaurant.Table.computer.ProximityPrompt
                    if prompt then memory_write("byte", prompt.Address + PromptEnabled, 1) end
                    task.wait(0.1)
                end
            end)
        end)

        local promptc = MainSec:Combo('prompt_type', 'Prompt Type',
            { 'ALL','Computer','Conveyor','Freezer','Toolbin','SeatingStation' }, 0,
            function(idx, text) end)

        MainSec:Button('Set instant prompt', function()
            local text = promptc:GetText()
            local ws   = game.Workspace
            local ra   = ws.RestaurantArea.restaurant

            local targets = {}
            if text == 'ALL' or text == 'Computer' then
                table.insert(targets, { path = ra.Table.computer.ProximityPrompt, loop = true })
            end
            if text == 'ALL' or text == 'Conveyor' then
                table.insert(targets, { path = ra.mainStuff.conveyors.keypad.PromptPart.ProximityPrompt })
            end
            if text == 'ALL' or text == 'Freezer' then
                table.insert(targets, { path = ra.FreezerDoor.PromptHitbox.ProximityPrompt })
            end
            if text == 'ALL' or text == 'Toolbin' then
                table.insert(targets, { path = ws.Toolbin.PromptHitbox.ProximityPrompt })
            end
            if text == 'ALL' or text == 'SeatingStation' then
                table.insert(targets, { path = ra["the waiter stand thingy"].Stations.SeatingStation.TriggerPart.SeatCustomersPrompt })
            end

            for _, t in ipairs(targets) do
                if not t.path then continue end
                if t.loop then
                    task.spawn(function()
                        while true do
                            memory_write("float", t.path.Address + HoldDuration, 0)
                            task.wait(0.1)
                        end
                    end)
                else
                    memory_write("float", t.path.Address + HoldDuration, 0)
                end
            end
        end)
    end

    local PlayerTab = tab:Section("Player", "Left", { "Player", "Notify" })

    if PlayerTab.page == 0 then
        PlayerTab:Toggle('inf_stamina', 'Infinite Stamina', function(v)
            if v then
                PlayerStats.StaminaUsageMultiplier.Value = -1
                PlayerStats.StaminaGainMultiplier.Value  = 100
                PlayerStats.StaminaUsageMultiplierNonHarvestRelated.Value = 0
            else
                PlayerStats.StaminaUsageMultiplier.Value = 1
                PlayerStats.StaminaUsageMultiplierNonHarvestRelated.Value = 1
            end
        end)
        PlayerTab:Tip('Does not work with hatchet or knife for harvesting meat.')
        PlayerTab:SliderInt('player_walkspeed', 'Walk Speed', 12, 20, 12, function(v)
            PlayerStats.WalkSpeed.Value = v
        end)
        PlayerTab:Combo('background', 'Career Background', background, 0, function(idx, text)
            LocalPlayer.CareerBackground.EquippedCareerBackground.Value = text
        end)
        PlayerTab:Tip('No "ForTheMoney" and "ForTheExperience", it will crash the game.')

    elseif PlayerTab.page == 1 then
        PlayerTab:Toggle('notify_admin_join', 'Admin Alert', function(v)
            SETTINGS.Notify.AdminJoin.Enabled = v
        end)
    end

    local NpcTab = tab:Section("Npc", "Left", { "Npc", "" })

    if NpcTab.page == 0 then
        NpcTab:Toggle('instant_seatcustomer', 'Instant Seat Customer', function(v)
            SETTINGS.Cheat.Instant.SeatCustomer.Enabled = v
        end)
        UI.SetValue('instant_seatcustomer', SETTINGS.Cheat.Instant.SeatCustomer.Enabled)

        NpcTab:Spacing(); NpcTab:Text("Visuals"); NpcTab:Spacing()

        NpcTab:Toggle('show_dasher', 'Dasher', function(v)
            if v and not SETTINGS.Cheat.Instant.SeatCustomer.Enabled then
                UI.SetValue('instant_seatcustomer', true)
                SETTINGS.Cheat.Instant.SeatCustomer.Enabled = true
            end
            ESP_SETTINGS.Dasher.Enabled = v
        end)
        NpcTab:ColorPicker("dasher_color", 1, 0, 0, 1, function(color)
            ESP_SETTINGS.Dasher.Color = Color3.new(color.R, color.G, color.B)
        end)
        NpcTab:Tip('Turn on Instant Seat Customer to make it work!')

        NpcTab:Toggle('show_armed', 'Armed npc', function(v) ESP_SETTINGS.Armed.Enabled = v end)
        NpcTab:ColorPicker("armed_color", 1, 0, 0, 1, function(color)
            ESP_SETTINGS.Armed.Color = Color3.new(color.R, color.G, color.B)
        end)

        NpcTab:Toggle('show_police', 'Police npc', function(v) ESP_SETTINGS.Police.Enabled = v end)
        NpcTab:ColorPicker("police_color", 0, 0, 1, 1, function(color)
            ESP_SETTINGS.Police.Color = Color3.new(color.R, color.G, color.B)
        end)

        NpcTab:Toggle('show_critic', 'Critic', function(v) ESP_SETTINGS.Critic.Enabled = v end)
        NpcTab:ColorPicker("critic_color", 1, 1, 0, 1, function(color)
            ESP_SETTINGS.Critic.Color = Color3.new(color.R, color.G, color.B)
        end)

        NpcTab:Toggle('show_health_inspector', 'Health Inspector', function(v)
            ESP_SETTINGS.HealthInspector.Enabled = v
        end)
        NpcTab:ColorPicker("health_inspector_color", 1, 0, 0, 1, function(color)
            ESP_SETTINGS.HealthInspector.Color = Color3.new(color.R, color.G, color.B)
        end)

        NpcTab:SliderInt('npc_esp_maxdist', 'Max Distance', 10, 200, 80, function(v)
            ESP_SETTINGS.MAX_DIST    = v
            ESP_SETTINGS.MAX_DIST_SQ = v * v
        end)
    end

    local CookingSec = tab:Section("Cooking", "Right", { "Cooking", "" })

    if CookingSec.page == 0 then
        CookingSec:SliderFloat('cooking_speed', 'Cooking Speed', 1.0, 5.0, 1.0, '%.1f', function(v)
            PlayerStats.CookingSpeedMultiplier.Value = v
        end)
        CookingSec:SliderInt('dupe_sushi_chance', 'Dupe Sushi Chance (%)', 0, 100, 0, function(v)
            PlayerStats.SushiDuplicationChance.Value = v
        end)
        CookingSec:SliderInt('conveyor_speed', 'Conveyor Speed', 1, 10, 1, function(v)
            GameData.ConveyorSpeedValue.Value = v
        end)
    end

    local WindowSec = tab:Section("Window", "Right")

    WindowSec:Toggle("show_statuswindow", "Enabled", function(v)
        ESP_SETTINGS.Windows.Enabled = v
        Windows:Show(v)
    end)
    WindowSec:Toggle("show_daycounter", "Day Counter", function(v)
        ESP_SETTINGS.Labels.day = v
        if v then dayLabel = Windows:AddLabel("Day: " .. tostring(GameData.CurrentDay.Value))
        elseif dayLabel then dayLabel:Delete(); dayLabel = nil end
    end)
    WindowSec:Toggle("show_waitinginline", "WaitingInLine Amount", function(v)
        ESP_SETTINGS.Labels.waitinginline = v
        if v then
            if not SETTINGS.Cheat.Instant.SeatCustomer.Enabled then
                UI.SetValue("instant_seatcustomer", true)
                SETTINGS.Cheat.Instant.SeatCustomer.Enabled = true
            end
            waitinginline = Windows:AddLabel("WaitingInLine: 0")
        elseif waitinginline then waitinginline:Delete(); waitinginline = nil end
    end)
    WindowSec:Toggle("show_company_money", "Company Money", function(v)
        ESP_SETTINGS.Labels.companymoney = v
        if v then companyMoneyLabel = Windows:AddLabel("Company Money: 0.00")
        elseif companyMoneyLabel then companyMoneyLabel:Delete(); companyMoneyLabel = nil end
    end)
    WindowSec:Toggle("show_vegstock", "Vegetables Stock", function(v)
        ESP_SETTINGS.Labels.vegstock = v
        if v then vegLabel = Windows:AddLabel("Vegetables: ?/?")
        elseif vegLabel then vegLabel:Delete(); vegLabel = nil end
    end)
    WindowSec:Toggle("show_fishstock", "Fish Stock", function(v)
        ESP_SETTINGS.Labels.fishstock = v
        if v then fishLabel = Windows:AddLabel("Fish: ?/?")
        elseif fishLabel then fishLabel:Delete(); fishLabel = nil end
    end)
    WindowSec:Toggle("show_humanfleshstock", "Human Flesh Stock", function(v)
        ESP_SETTINGS.Labels.humanfleshstock = v
        if v then HumanFlesh = Windows:AddLabel("Human Flesh: ?/?")
        elseif HumanFlesh then HumanFlesh:Delete(); HumanFlesh = nil end
    end)
    WindowSec:Toggle("show_ricestock", "Rice Stock", function(v)
        ESP_SETTINGS.Labels.ricestock = v
        if v then Rice = Windows:AddLabel("Rice: ?/?")
        elseif Rice then Rice:Delete(); Rice = nil end
    end)

    local InfoSec = tab:Section("Info", "Right")
    InfoSec:Text(
        "Version: 1.1.1\n" ..
        "changelog:\n" ..
        "[+] DishWashing Delay\n" ..
        "[~] Fixed DishWashing not working\n" ..
        "[~] Bug fixes & optimization\n"
    )
end)

-- Restore saved toggle states
SETTINGS.Auto.Cooking.Enabled        = UI.GetValue("auto_cooking")
SETTINGS.Auto.Washing.Enabled        = UI.GetValue("auto_washing")
SETTINGS.Auto.ChloroformSpys.Enabled = UI.GetValue("auto_chloroform_spys")
SETTINGS.Cheat.Instant.SeatCustomer.Enabled = UI.GetValue("instant_seatcustomer")
SETTINGS.Cheat.Instant.CleanDish.Enabled    = UI.GetValue("instant_clean_dish")
SETTINGS.Cheat.Spoof.Greenbar.Enabled       = UI.GetValue("spoof_greenbar")
SETTINGS.Cheat.Spoof.SpongeEffectiveness.Enabled = UI.GetValue("spoof_sponge_effectiveness")
SETTINGS.Notify.AdminJoin.Enabled    = UI.GetValue("notify_admin_join")

ESP_SETTINGS.Dasher.Enabled         = UI.GetValue("show_dasher")
ESP_SETTINGS.Armed.Enabled          = UI.GetValue("show_armed")
ESP_SETTINGS.Police.Enabled         = UI.GetValue("show_police")
ESP_SETTINGS.Critic.Enabled         = UI.GetValue("show_critic")
ESP_SETTINGS.HealthInspector.Enabled = UI.GetValue("show_health_inspector")
ESP_SETTINGS.Windows.Enabled        = UI.GetValue("show_statuswindow")
ESP_SETTINGS.Labels.day             = UI.GetValue("show_daycounter")
ESP_SETTINGS.Labels.waitinginline   = UI.GetValue("show_waitinginline")
ESP_SETTINGS.Labels.companymoney    = UI.GetValue("show_company_money")
ESP_SETTINGS.Labels.vegstock        = UI.GetValue("show_vegstock")
ESP_SETTINGS.Labels.fishstock       = UI.GetValue("show_fishstock")
ESP_SETTINGS.Labels.humanfleshstock = UI.GetValue("show_humanfleshstock")
ESP_SETTINGS.Labels.ricestock       = UI.GetValue("show_ricestock")

if ESP_SETTINGS.Windows.Enabled then Windows:Show(true) end

if ESP_SETTINGS.Labels.day then dayLabel = Windows:AddLabel("Day: " .. tostring(GameData.CurrentDay.Value)) end
if ESP_SETTINGS.Labels.waitinginline then waitinginline = Windows:AddLabel("WaitingInLine: 0") end
if ESP_SETTINGS.Labels.companymoney  then companyMoneyLabel = Windows:AddLabel("Company Money: 0.00") end
if ESP_SETTINGS.Labels.vegstock      then vegLabel    = Windows:AddLabel("Vegetables: ?/?") end
if ESP_SETTINGS.Labels.fishstock     then fishLabel   = Windows:AddLabel("Fish: ?/?") end
if ESP_SETTINGS.Labels.humanfleshstock then HumanFlesh = Windows:AddLabel("Human Flesh: ?/?") end
if ESP_SETTINGS.Labels.ricestock     then Rice        = Windows:AddLabel("Rice: ?/?") end

-- ─── Admin Alert ──────────────────────────────────────────────────────────

local Admin = { UserIds = { 105479622, 65095440, 1425697210, 126961971, 127528152, 319065798, 2754844524, 125440222, 1783725423 } }

Players.PlayerAdded:Connect(function(player)
    if table.find(Admin.UserIds, player.UserId) and SETTINGS.Notify.AdminJoin.Enabled then
        notify("Admin Joined: " .. player.Name, "ADMIN ALERT", 8)
    end
end)

-- ─── Auto Cooking ─────────────────────────────────────────────────────────

task.spawn(function()
    while true do
        if SETTINGS.Auto.Cooking.Enabled then
            local gui = PlayerGui:FindFirstChild("MakeSushiMinigame")
            if gui then
                local ok, visible = pcall(memory_read, "byte", gui.Address + ScreenGuiEnabled)
                if ok and visible == 1 then
                    local main = gui:FindFirstChild("MainFrame")
                    local frame = main and main:FindFirstChild("CuttingBarFrame")
                    if frame then
                        local slider   = frame.CutBar.Slider
                        local greenBar = frame.CutBar.GreenBar
                        local sx = slider.AbsolutePosition.X
                        local gx = greenBar.AbsolutePosition.X
                        if sx >= gx and sx <= gx + greenBar.AbsoluteSize.X then
                            keypress(0x43); keyrelease(0x43)
                        end
                    end
                end
            end
        end
        task.wait(0.05)
    end
end)

-- ─── Auto Washing ─────────────────────────────────────────────────────────

local key_map = { Q = 0x51, E = 0x45, R = 0x52, F = 0x46 }

task.spawn(function()
    while true do
        if SETTINGS.Auto.Washing.Enabled then
            local gui = PlayerGui:FindFirstChild("DishWashingGui")
            if gui then
                local sink   = game.Workspace.Kitchen:FindFirstChild("TheSink")
                local amount = sink and sink.DirtyDishAmount.Value
                if amount and amount > 0 then
                    local ok, visible = pcall(memory_read, "byte", gui.Address + ScreenGuiEnabled)
                    if ok and visible == 1 then
                        local holder = gui.MainFrame.QueueBarFrame.Queue.KeysHolder
                        for _, v in pairs(holder:GetChildren()) do
                            if v:IsA("Frame") and v.Name == "KeyTemplate" then
                                local vk = key_map[v:GetAttribute("Key")]
                                if vk then
                                    keypress(vk); keyrelease(vk)
                                    task.wait(SETTINGS.Auto.Washing.delay)
                                end
                            end
                        end
                    end
                end
            end
        end
        task.wait(0.1)
    end
end)

-- ─── Auto Chloroform ──────────────────────────────────────────────────────

task.spawn(function()
    while true do
        if SETTINGS.Auto.ChloroformSpys.Enabled then
            local Tool = Character and Character:FindFirstChild("Chloroform Spray")
            if Tool then
                local container = Tool.ChloroformChallenge.ButtonToHitFrame:FindFirstChild("Container")
                local label     = container and container:FindFirstChild("TextLabel")
                if label then
                    if label.Text == "Q" then keypress(0x51); keyrelease(0x51)
                    elseif label.Text == "E" then keypress(0x45); keyrelease(0x45) end
                end
            end
        end
        task.wait(0.01)
    end
end)

-- ─── Cheat Spoofs ─────────────────────────────────────────────────────────

task.spawn(function()
    while true do
        if SETTINGS.Cheat.Spoof.Greenbar.Enabled then
            PlayerStats.EffectiveCookingLevel.Value = 1000
            PlayerStats.NoIngredientUseChance.Value = 1
        end
        if SETTINGS.Cheat.Instant.CleanDish.Enabled then
            PlayerStats.EffectiveCleaningLevel.Value = 9999
        end
        if SETTINGS.Cheat.Spoof.SpongeEffectiveness.Enabled then
            PlayerStats.SpongeEffectiveness.Value = 9999
        end
        task.wait(0.1)
    end
end)

-- ─── Label Updater ────────────────────────────────────────────────────────

task.spawn(function()
    local storage = GameData.Ingredients.IngredientsStorage
    while true do
        if ESP_SETTINGS.Labels.waitinginline and waitinginline then
            local i = 0
            for _, npc in pairs(npc_cache) do -- iterate cache, not GetChildren()
                if npc.inst:GetAttribute("CurrentState") == "WaitingInLine" then i += 1 end
            end
            waitinginline:SetText("WaitingInLine: " .. i)
        end
        if ESP_SETTINGS.Labels.day and dayLabel then
            dayLabel:SetText("Day: " .. tostring(GameData.CurrentDay.Value))
        end
        if ESP_SETTINGS.Labels.companymoney and companyMoneyLabel then
            companyMoneyLabel:SetText("Company Money: " .. string.format("%.2f", GameData.CompanyMoney.Value))
        end
        if ESP_SETTINGS.Labels.vegstock and vegLabel then
            local s = storage.Vegetables
            vegLabel:SetText("Vegetables: " .. s.Stock.Value .. "/" .. s.MaxStock.Value)
        end
        if ESP_SETTINGS.Labels.fishstock and fishLabel then
            local s = storage.AssortedFish
            fishLabel:SetText("Fish: " .. s.Stock.Value .. "/" .. s.MaxStock.Value)
        end
        if ESP_SETTINGS.Labels.humanfleshstock and HumanFlesh then
            local s = storage.HumanFlesh
            HumanFlesh:SetText("Human Flesh: " .. s.Stock.Value .. "/" .. s.MaxStock.Value)
        end
        if ESP_SETTINGS.Labels.ricestock and Rice then
            local s = storage.Rice
            Rice:SetText("Rice: " .. s.Stock.Value .. "/" .. s.MaxStock.Value)
        end
        task.wait(0.1)
    end
end)

-- ─── NPC Logic (SeatCustomer + ESP) ──────────────────────────────────────

task.spawn(function()
    while true do
        if not isrbxactive() then task.wait(0.2) continue end

        for addr, cached in pairs(npc_cache) do
            local npc = cached.inst

            -- Instant Seat Customer — fixed: was checking table instead of .Enabled
            if SETTINGS.Cheat.Instant.SeatCustomer.Enabled
                and npc:GetAttribute("CurrentState") == "WaitingInLine"
                and not customer_list[addr] then
                local torso  = npc:FindFirstChild("Torso")
                local prompt = torso and torso:FindFirstChild("WaitProximityPrompt")
                if prompt and prompt.Address then
                    memory_write("float", prompt.Address + HoldDuration, 0)
                    customer_list[addr] = true
                end
            end

            -- Dasher
            if ESP_SETTINGS.Dasher.Enabled then
                if customer_list[addr] then
                    local torso  = npc:FindFirstChild("Torso")
                    local prompt = torso and torso:FindFirstChild("CatchDasherPrompt")
                    if prompt then
                        memory_write("float", prompt.Address + HoldDuration, 0)
                        if not Manager.Entities[addr] then
                            customer_list[addr] = nil
                            Manager:Add(addr, Entity.new(npc, Drawing.new("Text"), "Dasher"))
                        end
                    end
                end
            elseif Manager.Entities[addr] and Manager.Entities[addr].Type == "Dasher" then
                Manager:Remove(addr)
            end

            -- Police
            if ESP_SETTINGS.Police.Enabled and npc:GetAttribute("Cop") == true then
                if not Manager.Entities[addr] then
                    Manager:Add(addr, Entity.new(npc, Drawing.new("Text"), "Police"))
                end
            elseif Manager.Entities[addr] and Manager.Entities[addr].Type == "Police" then
                Manager:Remove(addr)
            end

            -- Armed
            if ESP_SETTINGS.Armed.Enabled and npc:GetAttribute("WeaponOfChoice") == "Knife" then
                if not Manager.Entities[addr] then
                    Manager:Add(addr, Entity.new(npc, Drawing.new("Text"), "Armed"))
                end
            elseif Manager.Entities[addr] and Manager.Entities[addr].Type == "Armed" then
                Manager:Remove(addr)
            end

            -- Critic
            if ESP_SETTINGS.Critic.Enabled and npc:GetAttribute("Critic") == true then
                if not Manager.Entities[addr] then
                    Manager:Add(addr, Entity.new(npc, Drawing.new("Text"), "Critic"))
                end
            elseif Manager.Entities[addr] and Manager.Entities[addr].Type == "Critic" then
                Manager:Remove(addr)
            end

            -- Health Inspector — fixed key name (no space)
            if ESP_SETTINGS.HealthInspector.Enabled and npc:GetAttribute("Behavior") == "Inspector" then
                if not Manager.Entities[addr] then
                    Manager:Add(addr, Entity.new(npc, Drawing.new("Text"), "HealthInspector"))
                end
            elseif Manager.Entities[addr] and Manager.Entities[addr].Type == "HealthInspector" then
                Manager:Remove(addr)
            end
        end

        -- Reset seating list at end of day
        if PlayerGui.clockGui.clockFrame.clockLabel.Text == "08:00" then
            customer_list = {}
        end

        task.wait(0.2)
    end
end)

-- ─── ESP Render (throttled to every 3 frames) ─────────────────────────────

local frameCount = 0
RunService.Heartbeat:Connect(function()
    frameCount += 1
    if frameCount % 3 ~= 0 then return end

    local root = Character and Character:FindFirstChild("HumanoidRootPart")

    for id, entity in pairs(Manager.Entities) do
        local cached = npc_cache[id]
        if not cached then Manager:Remove(id) continue end

        local head = cached.head
        if not head or not head.Parent then entity:Hide() continue end

        -- Fixed: HealthInspector key has no space now
        local cfg = ESP_SETTINGS[entity.Type]
        if not cfg then Manager:Remove(id) continue end
        if not cfg.Enabled then entity:Hide() continue end

        if root and (head.Position - root.Position).Magnitude > ESP_SETTINGS.MAX_DIST then
            entity:Hide()
            continue
        end

        entity:SetColor(cfg.Color)
        local pos, onScreen = WorldToScreen(head.Position + Vector3.new(0, 1.5, 0))
        entity:Update(pos, onScreen, true)
    end
end)

notify('Sushi Gambit loaded!', "", 3)
